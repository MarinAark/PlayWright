# CI环境专用依赖文件 - 只包含核心必需的依赖
# 核心测试框架
playwright==1.44.0
pytest>=7.0.0
pytest-html>=3.1.0
pytest-xdist>=2.5.0
pytest-asyncio>=0.21.0

# 报告和可视化
allure-pytest>=2.12.0

# HTTP和API测试
requests>=2.28.0
jsonschema>=4.0.0

# 配置和环境
python-dotenv>=0.19.0

# 基础工具
typing-extensions>=4.0.0

# 可选依赖 - 如果安装失败不会影响核心功能
# 这些依赖在代码中已经做了可选处理
